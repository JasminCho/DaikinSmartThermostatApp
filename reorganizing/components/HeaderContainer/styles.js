import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  hasBackButton: {
    marginBottom: 10,
    flexDirection: 'row',
    alignItems: 'center',
  },
  noBackButton: {
    paddingLeft: 30,
    marginBottom: 10,
    flexDirection: 'row',
    alignItems: 'center',
  },
})
